#include <stdio.h>
#include <stdlib.h>
#include <setjmp.h>

#include "tproc.h"
#include "jmp.h"



int
main ( int argc, char** argv )
{
  char up;
  /* jmp_buf buff, bufg; */

  top_stack = &up;
  init_sched();
  
  /* struct tproc *p1 = malloc(sizeof(struct tproc)); */
  /* struct tproc *p2 = malloc(sizeof(struct tproc)); */
  /* tproc[0] = *p1; */
  /* tproc[1] = *p2; */

  if ( mysetjmp(1) == 0)
    f();
  else
    g();

  return EXIT_SUCCESS;
}
